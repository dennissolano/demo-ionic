export interface Note {
  id: number;
  title: string;
  content: string;
  createDate: string;
  image: string;
}

export interface UserData {
  id: number;
  email: string;
  firstName: string;
  lastName: string;
  avatar: string;
}
